---
### This is a template for the dictionary containing login credentials used in the test environment
### The actual password file is in gitignore, as the Happy Robot test shop is for our use only

users:

    # these are for test customer login
    customer:
        username: customer
        password: customerpassword

    # for test shop manager login (WooCommerce shop manager, not Wordpress admin! Can edit orders etc. though)
    manager:
        username: manager
        password: managerpassword
        display_name: Manager      # name of the manager :) used for checking login success

    # these are for test admin login (Wordpress site admin, not real as they are not used in the test)
    admin:
        username: admin
        password: adminmultipass
